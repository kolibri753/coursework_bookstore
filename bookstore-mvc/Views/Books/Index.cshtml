@model IEnumerable<Book>

@{
    ViewData["Title"] = "List of Books";
}

@* <div class="display">
<div>@Html.DisplayNameFor(model => model.Title)</div>
  <div>@Html.DisplayNameFor(model => model.Pages)</div>
  <div>@Html.DisplayNameFor(model => model.Price)</div>
  <div>@Html.DisplayNameFor(model => model.Quantity)</div>
  <div>@Html.DisplayNameFor(model => model.Genre)</div>
  <div>@Html.DisplayNameFor(model => model.Author)</div>
</div> *@

<div class="section gallery">
  <h1 class="gallery__title section-title">List of Books</h1>

  @foreach(var item in Model) 
  {
    @* <p class="text">@item.Title - title</p> *@
    @* <p class="text">@item.Genre - Genre</p> *@
    <div class="gallery__book book">
      <div class="act-list">
        <a class="btn reversed act-item" asp-controller="Books" asp-action="Edit" asp-route-id="@item.Id">
				  <i class="fa-solid fa-pen-to-square"></i>
          <span class="btn__text">Update</span>
        </a>
        <a class="btn reversed act-item" asp-controller="Books" asp-action="Delete" asp-route-id="@item.Id">
          <i class="fa-solid fa-trash"></i>
          <span class="btn__text">Delete</span>
        </a>
      </div>
      <button class="act-btn">
        <i class="fa-solid fa-bars"></i>
      </button>
      @* <div class="section__buttons">
        <button class="btn">
    	    <i class="fa-solid fa-cart-shopping"></i>
  		  </button>
        <button class="btn">
    	    <i class="fa-solid fa-cart-shopping"></i>
  		  </button>
      </div> *@
      <img class="book__cover" src="@item.ImageURL" alt="cover">
      <div class="book__info">
        <h3 class="book__title">@item.Title</h3>
        <span class="book__author">@item.Author</span>
      </div>
        <a class="btn" asp-action="Info" asp-route-id="@item.Id"> @* asp-controller="Orders" asp-action="Buy" *@
          @* <span class="btn__text">Info</span> *@
          <span class="btn__text">@item.Price₴</span>
          @* <i class="fa-solid fa-cart-shopping"></i> *@
          <i class="fa-solid fa-circle-info"></i>
        </a>

    </div>

    @* <p class="text-html">@Html.DisplayFor(modelItem => item.Title)</p> *@

  }
</div>

<a class="fixed-btn" asp-controller="Books" asp-action="Create">
  <span class="btn__text">Add New</span>
  <i class="fa-solid fa-plus"></i>
</a>